<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nova | Dashboard</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root { --gold: #D4AF37; --dark: #0b0e11; --panel: #1e2329; --border: #2b3139; --text: #eaecef; }
        
        body { background: var(--dark); color: var(--text); font-family: 'DINPro', sans-serif; margin: 0; padding: 0; }
        
        /* HEADER */
        .app-header { display: flex; align-items: center; justify-content: space-between; padding: 15px 20px; background: var(--dark); position: sticky; top: 0; z-index: 100; border-bottom: 1px solid var(--border); }
        .user-greet { font-size: 14px; color: #848e9c; }
        .user-name { font-weight: bold; color: white; font-size: 16px; }
        .menu-btn { font-size: 20px; color: white; cursor: pointer; }

        /* SIDEBAR (Hamburger) */
        .sidebar-overlay { position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.8); z-index: 999; display: none; }
        .sidebar { position: fixed; top: 0; left: -280px; width: 280px; height: 100%; background: #161a1e; z-index: 1000; transition: 0.3s; display: flex; flex-direction: column; padding-top: 40px; }
        .sidebar.open { left: 0; }
        .sb-link { padding: 15px 30px; color: var(--text); text-decoration: none; font-size: 16px; display: flex; align-items: center; gap: 15px; }
        .sb-link i { width: 25px; color: var(--gold); }
        .sb-link:hover { background: rgba(255,255,255,0.05); }

        /* MAIN BODY */
        .container { padding: 20px; }

        /* BALANCE CARD */
        .balance-card { background: linear-gradient(135deg, #2b3139 0%, #161a1e 100%); border-radius: 15px; padding: 25px; text-align: center; box-shadow: 0 10px 30px rgba(0,0,0,0.3); margin-bottom: 30px; border: 1px solid rgba(255,255,255,0.05); }
        .bal-label { font-size: 12px; color: #848e9c; letter-spacing: 1px; text-transform: uppercase; }
        .bal-val { font-size: 40px; font-weight: bold; font-family: 'Roboto Mono'; margin: 10px 0; color: white; }
        
        /* QUICK ACTIONS (Circles) */
        .quick-actions { display: flex; justify-content: space-around; margin-bottom: 30px; }
        .qa-item { display: flex; flex-direction: column; align-items: center; gap: 8px; text-decoration: none; color: #848e9c; font-size: 12px; }
        .qa-icon { width: 50px; height: 50px; background: rgba(212, 175, 55, 0.1); border-radius: 50%; display: flex; align-items: center; justify-content: center; color: var(--gold); font-size: 20px; transition: 0.2s; border: 1px solid rgba(212, 175, 55, 0.2); }
        .qa-item:active .qa-icon { background: var(--gold); color: black; }

        /* APP GRID (Bondex Style) */
        .section-header { font-size: 16px; font-weight: bold; margin-bottom: 15px; display: flex; justify-content: space-between; align-items: center; }
        .app-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 15px; }
        .grid-card { background: var(--panel); padding: 20px; border-radius: 12px; text-decoration: none; display: flex; flex-direction: column; gap: 10px; border: 1px solid var(--border); transition: 0.2s; }
        .grid-card:hover { transform: translateY(-2px); border-color: var(--gold); }
        .gc-icon { font-size: 24px; color: var(--gold); }
        .gc-title { font-size: 14px; font-weight: bold; color: white; }
        .gc-desc { font-size: 11px; color: #848e9c; }

    </style>
</head>
<body>

    <div class="sidebar-overlay" id="overlay" onclick="toggleMenu()"></div>
    <div class="sidebar" id="sidebar">
        <div style="padding:0 30px 30px 30px; font-size:24px; font-weight:bold; color:var(--gold);">NOVA</div>
        <a href="/dashboard" class="sb-link"><i class="fas fa-home"></i> Home</a>
        <a href="/trade" class="sb-link"><i class="fas fa-chart-line"></i> Trade</a>
        <a href="/deposit" class="sb-link"><i class="fas fa-wallet"></i> Wallet</a>
        <a href="/profile" class="sb-link"><i class="fas fa-user"></i> Profile</a>
        <a href="/logout" class="sb-link" style="color:#f6465d;"><i class="fas fa-power-off"></i> Logout</a>
    </div>

    <div class="app-header">
        <div>
            <div class="user-greet">Welcome back,</div>
            <div class="user-name"><%= user.name %></div>
        </div>
        <i class="fas fa-bars menu-btn" onclick="toggleMenu()"></i>
    </div>

    <div class="container">
        <div class="balance-card">
            <div class="bal-label">Total Equity</div>
            <div class="bal-val">$<%= user.balance.toFixed(2) %></div>
        </div>

        <div class="quick-actions">
            <a href="/deposit" class="qa-item">
                <div class="qa-icon"><i class="fas fa-plus"></i></div>
                <span>Deposit</span>
            </a>
            <a href="/withdraw" class="qa-item">
                <div class="qa-icon"><i class="fas fa-arrow-up"></i></div>
                <span>Withdraw</span>
            </a>
            <a href="/trade" class="qa-item">
                <div class="qa-icon"><i class="fas fa-exchange-alt"></i></div>
                <span>Trade</span>
            </a>
            <a href="/profile" class="qa-item">
                <div class="qa-icon"><i class="fas fa-user-cog"></i></div>
                <span>Settings</span>
            </a>
        </div>

        <div class="section-header">
            <span>Explore</span>
        </div>
        <div class="app-grid">
            <a href="/trade" class="grid-card">
                <i class="fas fa-chart-area gc-icon"></i>
                <div class="gc-title">Live Markets</div>
                <div class="gc-desc">Trade BTC, ETH & indices with 50x leverage.</div>
            </a>
            <a href="#" class="grid-card">
                <i class="fas fa-copy gc-icon" style="color:#0ecb81;"></i>
                <div class="gc-title">Copy Trading</div>
                <div class="gc-desc">Coming Soon. Mimic top traders automatically.</div>
            </a>
            <a href="/deposit" class="grid-card">
                <i class="fas fa-coins gc-icon" style="color:#3d8bfd;"></i>
                <div class="gc-title">Staking</div>
                <div class="gc-desc">Earn 5% APY on your idle assets.</div>
            </a>
            <a href="#" class="grid-card">
                <i class="fas fa-robot gc-icon" style="color:#ff6b6b;"></i>
                <div class="gc-title">AI Signals</div>
                <div class="gc-desc">Coming Soon. Smart entry points.</div>
            </a>
        </div>
    </div>

    <script>
        function toggleMenu() {
            document.getElementById('sidebar').classList.toggle('open');
            document.getElementById('overlay').style.display = document.getElementById('sidebar').classList.contains('open') ? 'block' : 'none';
        }
    </script>
</body>
</html>