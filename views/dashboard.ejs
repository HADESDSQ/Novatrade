<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NovaCapital | Dashboard</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root { --gold: #D4AF37; --dark: #0b0e11; --panel: #1e2329; --border: #2b3139; --text: #eaecef; }
        
        body { background: var(--dark); color: var(--text); font-family: 'DINPro', sans-serif; margin: 0; display: flex; min-height: 100vh; }
        
        /* SIDEBAR (Desktop) */
        .sidebar { width: 250px; background: var(--panel); border-right: 1px solid var(--border); padding: 20px; display: flex; flex-direction: column; }
        .logo { color: var(--gold); font-size: 22px; font-weight: bold; margin-bottom: 40px; }
        .nav-link { display: flex; align-items: center; padding: 12px 15px; color: #848e9c; text-decoration: none; border-radius: 8px; margin-bottom: 8px; transition: 0.2s; }
        .nav-link:hover, .nav-link.active { background: rgba(212, 175, 55, 0.1); color: var(--gold); }
        .nav-link i { width: 25px; }

        /* MAIN CONTENT */
        .main-content { flex: 1; padding: 30px; overflow-y: auto; padding-bottom: 80px; /* Space for mobile nav */ }
        .welcome { font-size: 24px; margin-bottom: 20px; }
        
        /* EQUITY CARD */
        .balance-card { background: linear-gradient(135deg, #1e2329 0%, #161a1e 100%); border: 1px solid var(--gold); border-radius: 12px; padding: 30px; margin-bottom: 30px; position: relative; overflow: hidden; }
        .balance-label { color: #848e9c; font-size: 14px; text-transform: uppercase; letter-spacing: 1px; }
        .balance-amount { font-size: 42px; font-weight: bold; color: white; margin: 10px 0 20px 0; font-family: 'Roboto Mono', monospace; }
        
        .action-btn-group { display: flex; gap: 15px; }
        .btn { padding: 12px 25px; border-radius: 6px; font-weight: bold; text-decoration: none; cursor: pointer; border: none; font-size: 14px; }
        .btn-gold { background: var(--gold); color: black; }
        .btn-outline { border: 1px solid #444; color: white; background: transparent; }

        /* TRANSACTIONS TABLE */
        .tx-section { background: var(--panel); border-radius: 12px; padding: 20px; }
        .table-responsive { overflow-x: auto; }
        table { width: 100%; border-collapse: collapse; min-width: 500px; }
        th { text-align: left; color: #848e9c; padding: 15px; font-size: 12px; border-bottom: 1px solid var(--border); }
        td { padding: 15px; border-bottom: 1px solid var(--border); font-size: 14px; }
        .status-badge { padding: 4px 8px; border-radius: 4px; font-size: 11px; font-weight: bold; text-transform: uppercase; }
        
        /* MOBILE BOTTOM NAV (Hidden by default) */
        .mobile-nav { display: none; position: fixed; bottom: 0; left: 0; width: 100%; background: var(--panel); border-top: 1px solid var(--border); z-index: 1000; padding-bottom: env(safe-area-inset-bottom); }
        .mobile-nav-item { flex: 1; display: flex; flex-direction: column; align-items: center; justify-content: center; padding: 12px 0; color: #848e9c; text-decoration: none; font-size: 10px; }
        .mobile-nav-item i { font-size: 18px; margin-bottom: 4px; }
        .mobile-nav-item.active { color: var(--gold); }

        /* RESPONSIVE CSS */
        @media (max-width: 768px) {
            .sidebar { display: none; } /* Hide Sidebar */
            .mobile-nav { display: flex; } /* Show Bottom Nav */
            .main-content { padding: 20px; }
            .balance-amount { font-size: 32px; }
            .btn { flex: 1; text-align: center; }
            
            /* Stack table for mobile */
            .tx-section { background: transparent; padding: 0; }
            .tx-section h3 { margin-bottom: 15px; font-size: 18px; }
            table, thead, tbody, th, td, tr { display: block; }
            thead tr { position: absolute; top: -9999px; left: -9999px; }
            tr { background: var(--panel); border: 1px solid var(--border); border-radius: 8px; margin-bottom: 15px; padding: 15px; }
            td { border: none; padding: 5px 0; display: flex; justify-content: space-between; }
            td:before { content: attr(data-label); color: #848e9c; font-weight: bold; margin-right: 10px; }
        }
    </style>
</head>
<body>

    <div class="sidebar">
        <div class="logo">NOVACAPITAL</div>
        <a href="/dashboard" class="nav-link active"><i class="fas fa-home"></i> Dashboard</a>
        <a href="/trade" class="nav-link"><i class="fas fa-chart-line"></i> Live Trading</a>
        <a href="/deposit" class="nav-link"><i class="fas fa-wallet"></i> Deposit Funds</a>
        <a href="/withdraw" class="nav-link"><i class="fas fa-money-bill-wave"></i> Withdraw</a>
        <a href="/logout" class="nav-link" style="margin-top:auto;"><i class="fas fa-sign-out-alt"></i> Logout</a>
    </div>

    <div class="main-content">
        <div class="welcome">
            Welcome back,<br>
            <span style="font-weight:bold;"><%= user.name.split(' ')[0] %></span>
        </div>

        <div class="balance-card">
            <div class="balance-label">Total Equity</div>
            <div class="balance-amount">$<%= user.balance.toFixed(2) %></div>
            <div class="action-btn-group">
                <form action="/deposit" method="POST" style="flex:1; display:none;"> 
                    </form>
                <a href="/deposit" class="btn btn-gold"><i class="fas fa-plus"></i> Deposit</a>
                <a href="/withdraw" class="btn btn-outline"><i class="fas fa-arrow-up"></i> Withdraw</a>
            </div>
        </div>

        <div class="tx-section">
            <h3>Recent Transactions</h3>
            <% if(transactions.length === 0) { %>
                <div style="color:#666; font-style:italic;">No transactions yet.</div>
            <% } else { %>
                <div class="table-responsive">
                    <table>
                        <thead>
                            <tr>
                                <th>Type</th>
                                <th>Amount</th>
                                <th>Method</th>
                                <th>Status</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% transactions.forEach(t => { %>
                            <tr>
                                <td data-label="Type" style="text-transform:capitalize;"><%= t.type %></td>
                                <td data-label="Amount">$<%= t.amount %></td>
                                <td data-label="Method"><%= t.method %></td>
                                <td data-label="Status">
                                    <span class="status-badge" style="
                                        background:<%= t.status=='approved'?'rgba(14,203,129,0.15)':(t.status=='rejected'?'rgba(246,70,93,0.15)':'rgba(240,185,11,0.15)') %>;
                                        color:<%= t.status=='approved'?'#0ecb81':(t.status=='rejected'?'#f6465d':'#f0b90b') %>">
                                        <%= t.status %>
                                    </span>
                                </td>
                            </tr>
                            <% }) %>
                        </tbody>
                    </table>
                </div>
            <% } %>
        </div>
    </div>

    <div class="mobile-nav">
        <a href="/dashboard" class="mobile-nav-item active">
            <i class="fas fa-home"></i>
            <span>Home</span>
        </a>
        <a href="/trade" class="mobile-nav-item">
            <i class="fas fa-chart-line"></i>
            <span>Trade</span>
        </a>
        <a href="/deposit" class="mobile-nav-item">
            <i class="fas fa-wallet"></i>
            <span>Deposit</span>
        </a>
        <a href="/logout" class="mobile-nav-item">
            <i class="fas fa-user"></i>
            <span>Profile</span>
        </a>
    </div>

</body>
</html>