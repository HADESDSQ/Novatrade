<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root { --gold: #D4AF37; --dark: #0b0e11; --panel: #1e2329; --border: #2b3139; --text: #eaecef; }
        body { background: var(--dark); color: var(--text); font-family: 'DINPro', sans-serif; margin: 0; padding: 20px; padding-bottom: 80px; }
        
        .profile-header { text-align: center; margin-bottom: 30px; margin-top: 40px; }
        .avatar { width: 80px; height: 80px; background: var(--gold); border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 30px; color: black; font-weight: bold; margin: 0 auto 15px auto; }
        .user-name { font-size: 24px; font-weight: bold; }
        .user-email { color: #848e9c; font-size: 14px; margin-top: 5px; }

        .menu-list { background: var(--panel); border-radius: 12px; overflow: hidden; border: 1px solid var(--border); }
        .menu-item { display: flex; align-items: center; padding: 15px 20px; border-bottom: 1px solid var(--border); text-decoration: none; color: var(--text); transition: 0.2s; }
        .menu-item:last-child { border-bottom: none; }
        .menu-item:active { background: #2b3139; }
        .menu-icon { width: 30px; color: var(--gold); }
        .menu-text { flex: 1; font-size: 14px; }
        .menu-arrow { color: #848e9c; font-size: 12px; }

        .logout-btn { display: block; width: 100%; padding: 15px; background: #f6465d; color: white; text-align: center; border-radius: 12px; text-decoration: none; font-weight: bold; margin-top: 20px; }

        /* MOBILE NAV */
        .mobile-nav { display: flex; position: fixed; bottom: 0; left: 0; width: 100%; background: var(--panel); border-top: 1px solid var(--border); z-index: 1000; padding-bottom: env(safe-area-inset-bottom); }
        .mobile-nav-item { flex: 1; display: flex; flex-direction: column; align-items: center; justify-content: center; padding: 12px 0; color: #848e9c; text-decoration: none; font-size: 10px; }
        .mobile-nav-item i { font-size: 18px; margin-bottom: 4px; }
        .mobile-nav-item.active { color: var(--gold); }
    </style>
</head>
<body>

    <div class="profile-header">
        <div class="avatar"><%= user.name.charAt(0) %></div>
        <div class="user-name"><%= user.name %></div>
        <div class="user-email"><%= user.email %></div>
    </div>

    <div class="menu-list">
        <div class="menu-item">
            <i class="fas fa-wallet menu-icon"></i>
            <span class="menu-text">Balance: $<%= user.balance.toFixed(2) %></span>
        </div>
        <a href="/deposit" class="menu-item">
            <i class="fas fa-plus-circle menu-icon"></i>
            <span class="menu-text">Deposit Funds</span>
            <i class="fas fa-chevron-right menu-arrow"></i>
        </a>
        <a href="/withdraw" class="menu-item">
            <i class="fas fa-arrow-circle-up menu-icon"></i>
            <span class="menu-text">Withdraw Funds</span>
            <i class="fas fa-chevron-right menu-arrow"></i>
        </a>
        <a href="https://t.me/novasupport" class="menu-item"> <i class="fas fa-headset menu-icon"></i>
            <span class="menu-text">Support</span>
            <i class="fas fa-chevron-right menu-arrow"></i>
        </a>
    </div>

    <a href="/logout" class="logout-btn">Log Out</a>

    <div class="mobile-nav">
        <a href="/dashboard" class="mobile-nav-item"><i class="fas fa-home"></i><span>Home</span></a>
        <a href="/trade" class="mobile-nav-item"><i class="fas fa-chart-line"></i><span>Trade</span></a>
        <a href="/deposit" class="mobile-nav-item"><i class="fas fa-wallet"></i><span>Deposit</span></a>
        <a href="/profile" class="mobile-nav-item active"><i class="fas fa-user"></i><span>Profile</span></a>
    </div>

</body>
</html>